1. Car:
    States: currentSpeed, gearPosition, mileage
    Behaviors: shiftGear(), lockDoor(), startEngine()

2. Phone:
    States: batteryLevel, brand, storageUsed
    Behaviors: installApp(), takePhoto(), makeCall()

3. AirConditioner:
    States: mode, fanSpeed, currentTemperature
    Behaviors: turnOn(), changeMode(), setTemperture()

4. Tv:
    States: isMuted, screenSize, volumeLevel
    Behaviors: switchInput(), changeChannel(), increaseVolume()

5. CoffeeMachine:
    States: waterLevel, beanLevel, temperature
    Behaviors: refillWater(), displayStatus(), turnOn()

6. SmartWatch:
    States: heartRate, brand, batteryLevel
    Behaviors: showNotification(), checkBattery(), trackSteps()

7. Microwave:
    States: currentMode, powerLevel, brand
    Behaviors: setTimer(), setPowerLevel(), changeMode()

8. Printer:
    States: inkLevel, paperCount, connectionType
    Behaviors: addPaper(), refillInk(), turnOff()

9. Hourse: 
    States: name, age, color
    Behaviors: eating(), sleeping(), running()

10. Camera:
     States: lensType, batteryLevel, mode
     Behaviors: takePhoto(), changeMode(), deletMedia()

11. WashingMachine:
     States: waterLevel, remainingTime, loadSize
     Behaviors: drainWater(), startWash(), turnOn()

12. Drone:
     States: isFlying, batteryLevel, gpsLocation
     Behaviors: recordVideo(), returnToHome(), takeOff()

13. SmartLamp:
     States: brand, isOn, color
     Behaviors: changeColor(), connectToApp(), setMode()